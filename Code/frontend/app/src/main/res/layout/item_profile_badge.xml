<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    app:cardBackgroundColor="@color/md_surface"
    app:cardCornerRadius="20dp"
    app:cardElevation="0dp"
    app:strokeColor="@color/md_outline"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="14dp">

        <FrameLayout
            android:id="@+id/viewBadgeArt"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:background="@drawable/bg_badge_common">

            <ImageView
                android:id="@+id/ivBadgeIcon"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:layout_gravity="center"
                android:contentDescription="@null"
                android:src="@android:drawable/ic_menu_agenda"
                app:tint="@color/app_warning_text" />
        </FrameLayout>

        <TextView
            android:id="@+id/tvBadgeTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:fontFamily="sans-serif-medium"
            android:textColor="@color/md_on_surface"
            android:textSize="14sp"
            android:textStyle="bold"
            tools:text="First Task" />

        <TextView
            android:id="@+id/tvBadgeRarity"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:background="@drawable/bg_metric_tile"
            android:paddingStart="10dp"
            android:paddingTop="4dp"
            android:paddingEnd="10dp"
            android:paddingBottom="4dp"
            android:textColor="@color/md_on_surface_variant"
            android:textSize="11sp"
            android:textStyle="bold"
            tools:text="Common" />

        <TextView
            android:id="@+id/tvBadgeStatus"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:lineSpacingExtra="2dp"
            android:textColor="@color/md_on_surface_variant"
            android:textSize="12sp"
            tools:text="Locked • Complete your first task" />
    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
